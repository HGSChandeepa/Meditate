import 'package:flutter/material.dart';

class MindfulExercisesTab extends StatelessWidget {
  const MindfulExercisesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mindful Exercises List"),
    );
  }
}
