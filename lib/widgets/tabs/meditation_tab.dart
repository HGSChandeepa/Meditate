import 'package:flutter/material.dart';

class MeditationsTab extends StatelessWidget {
  const MeditationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Meditations List"),
    );
  }
}