import 'package:flutter/material.dart';

class SleepExercisesTab extends StatelessWidget {
  const SleepExercisesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sleep Exercises List"),
    );
  }
}
